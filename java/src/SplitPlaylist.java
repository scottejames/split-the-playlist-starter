/**
 * Split the Playlist.
 *
 * Implement minLongestSide below. See ../../README.md for the full
 * problem statement, constraints, and worked examples.
 *
 * The helper methods are optional scaffolding — use them, change their
 * signatures, or delete them and structure your solution however you like.
 */
public class SplitPlaylist {

    /** Return the combined duration of every song. */
    static long totalDuration(int[] songs) {
        // TODO: implement
        throw new UnsupportedOperationException("not implemented");
    }

    /** Return the duration of the longest individual song. */
    static int longestSingleSong(int[] songs) {
        // TODO: implement
        throw new UnsupportedOperationException("not implemented");
    }

    /**
     * Split songs into exactly d contiguous, non-empty groups (in their
     * original order — songs can't be reordered or split apart), so as
     * to minimize the largest total duration among the d groups.
     *
     * Return that minimum possible "longest side" duration, or -1 if
     * there are more sides than songs.
     */
    static long minLongestSide(int[] songs, int d) {
        // TODO: implement
        throw new UnsupportedOperationException("not implemented");
    }
}
